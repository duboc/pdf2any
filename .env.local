# Google Cloud Platform Configuration
GCP_PROJECT_ID=projectid
GCS_BUCKET_NAME=yourbucket
VERTEX_AI_LOCATION="us-central1" # Or your preferred GCP region

# Option 1: Service Account Key File (Recommended for local development/testing)
# Provide the absolute path to your service account key JSON file.
# Ensure this file is NOT committed to version control.
#GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account-key.json"

# Option 2: Application Default Credentials (ADC) (Recommended for Cloud Run/Functions/GCE)
# If running in a GCP environment with ADC configured, you might not need GOOGLE_APPLICATION_CREDENTIALS.
# Comment out the GOOGLE_APPLICATION_CREDENTIALS line above if using ADC.

# Logging Level (Optional, examples: DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL="INFO"
